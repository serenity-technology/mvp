@page "/input/amount"

<Page Label="Input Amount Component" Title="UI Examples - Input Amount">
    <Body>
        <Form class="w-60 flex flex-col gap-1">
            <Amount Label="Amount Enabled" @bind-Value="_amount" />
            <Amount Label="Amount Disabled" @bind-Value="_amount" Disabled />
        </Form>
    </Body>
</Page>

@code {
    private decimal _amount;
}